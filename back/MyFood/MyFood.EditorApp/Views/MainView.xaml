<Page
    x:Class="MyFood.EditorApp.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyFood.EditorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    xmlns:viewModels="using:MyFood.EditorApp.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
    NavigationCacheMode="Disabled"
    mvvm:ViewModelLocator.AutoWireViewModel="True">


    <GridView ItemsSource="{Binding Previews}">
        <DataTemplate x:DataType="viewModels:PreviewViewModel">
            <Grid>
                <Image Source="{Binding PreviewUrl}" Height="180" Width="180" Stretch="UniformToFill"/>
                <TextBlock HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Text="{Binding Title}" FontWeight="Bold"/>
            </Grid>
        </DataTemplate>
    </GridView>
</Page>
